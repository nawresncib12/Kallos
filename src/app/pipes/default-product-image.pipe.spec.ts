import { DefaultProductImagePipe } from './default-product-image.pipe';

describe('DefaultProductImagePipe', () => {
  it('create an instance', () => {
    const pipe = new DefaultProductImagePipe();
    expect(pipe).toBeTruthy();
  });
});
